# Copy to `.env` (same folder or repo root) and fill in. Loaded automatically by
# the CLIs; real environment variables take precedence over this file.
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
